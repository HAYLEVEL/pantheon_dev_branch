pipelines:
  branches:
    CDtest:
    #'*.dev':
      - step:
          name: "Deploy_Pantheon"
          runs-on:
            - cd
          image: alpine:latest
          script:
            - apk add git
            - git config --global --add safe.directory $BITBUCKET_CLONE_DIR
            - git --version
            - curl --version
