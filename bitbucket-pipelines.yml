image: atlassian/default-image:latest

pipelines:
  default:
    - step:
        name: Test
        runs-on:
          - linux
        script:
          - echo "linux"
  - step:
        name: CI
        runs-on:
          - ci
        script:
          - echo "ci"
  - step:
        name: CD
        runs-on:
          - cd
        script:
          - echo "cd"
